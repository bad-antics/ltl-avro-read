// ltl-avro-read/main.ltl
//
// Apache Avro binary-format decoder with schema.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-avro-read v0.1.0")
    io::println("Apache Avro binary-format decoder with schema.")
    0
}
